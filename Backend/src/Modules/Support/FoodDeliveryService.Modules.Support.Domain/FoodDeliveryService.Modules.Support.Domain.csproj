<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\..\..\Common\FoodDeliveryService.Common.Domain\FoodDeliveryService.Common.Domain.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Ticket.SetAssignedAgent is the internal assignee write path, wrapped by the guarded
         Claim/AssignTo/Unassign methods. The unit tests also reach it directly, to build tickets in
         the assignment-dependent states (InProgress, Resolved, Closed) without walking the whole
         lifecycle first. -->
    <InternalsVisibleTo Include="FoodDeliveryService.Modules.Support.UnitTests" />
  </ItemGroup>

</Project>
