<Project Sdk="Microsoft.NET.Sdk">

  <!-- FraudDetection's outbound contract surface. Empty in Milestone A by design: the service only consumes
       in this milestone, and FraudAlertRaised/FraudAlertResolved arrive with the alert aggregate in
       Milestone C. The project exists now so the module has the same five-project shape as every
       other one, and so the Application layer's reference is already in place. -->

  <ItemGroup>
    <ProjectReference Include="..\..\..\Common\FoodDeliveryService.Common.Application\FoodDeliveryService.Common.Application.csproj" />
  </ItemGroup>

</Project>
