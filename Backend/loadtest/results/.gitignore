# Run artifacts are regenerated by every run and are not worth reviewing in a diff.
#
# The exception is `published/` — Milestone H commits the specific summaries the README and
# docs/load-testing.md quote, because Prometheus keeps seven days on a disposable volume and a
# published number with no artifact behind it is an assertion, not evidence.
*
!.gitignore
!published/
!published/**
